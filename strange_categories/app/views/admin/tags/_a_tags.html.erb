<% data ||= @data %>
<% available_tags ||= @available_tags %>

<% available_tags.each do |tag| %>
	
<%= link_to raw("<div class='selected_tag_deleta'>#{ fineline_icon('neu') }</div><div class='selected_tag_name'>#{ tag.name }</div><div class='clearfix'></div>"), 
							"/admin/#{data.class.to_s.tableize}/#{data.id}/tags/#{tag.id}/select_data_tags",
							# => :id => dom_id(tag),
							:class => 'available_tag',
							:remote => true %>
		
		
<% end %>