<div id="fine_header">
	
	<div class="container_48">
		
		<div class="fl_box_950">
				<% if Strangecms::Config[:use_logo_datei] && Strangecms::Config[:logo_datei] && !Strangecms::Config[:logo_datei].blank? %>
						<% if datei = Datei.find(Strangecms::Config[:logo_datei]) %>
							<%= link_to( image_tag( datei.file.thumb("100x350>").url() ), root_url, :id => "fineLOGO" ) %>
						<% else %>
							<%= link_to( theme_image_tag("#{Strangecms::Config[:strange_logo]}"), root_url, :id => "fineLOGO" ) %>
						<% end %>
				<% else %>
					<%= link_to( theme_image_tag("#{Strangecms::Config[:strange_logo]}"), root_url, :id => "fineLOGO" ) %>
				<% end %>
			
				
		

		
				<%= render 'navigation/header' %>
		
		</div>

		<div class="clearfix"></div>
	
	</div>
</div>
