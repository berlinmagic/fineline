<%= render 'admin/shared/module_sub_menu' %>

<div class="admin_box">
	<h1>(Bild-) Kategorien</h1>
	<div class="admin_in_box">
		<div class="box">
			<div class="in_box">
				<% if @bild_kategorien.count > 1 %>
					<h2>Aktuell sind <%= @bild_kategorien.count %> Kategorien vorhanden.</h2>
				<% elsif @bild_kategorien.count == 1 %>
					<h2>Aktuell ist <%= @bild_kategorien.count %> Kategorie vorhanden.</h2>
				<% else %>
					<h2>Aktuell sind keine Kategorien vorhanden.</h2>
				<% end %>
			</div>
		</div>
		<div class="box">
			<div class="trenn_box">
				<div class="top_trenner"></div>
			</div>
		</div>
		<div class="box">
			<div class="in_box white">
				<br/>
				<% @bild_kategorien.each do |bk| %>
					
					<%= render 'admin/bild_kategorien/kat', :bk => bk %>
					
				<% end %>
				<div class="clearfix"></div>
				<br/>
			</div>
		</div>
		
	</div>
</div>

<%= render 'admin/gallerien/sidebar' %>